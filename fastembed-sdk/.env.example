# FastEmbed SDK Environment Configuration Template
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the placeholder values with your actual API credentials
# 3. The .env file is gitignored for security
#
# Copy this file to .env and add your API credentials
# DO NOT commit .env to git - it's in .gitignore

# FastEmbed Server (local)
FASTEMBED_BASE_URL=http://127.0.0.1:8000

# Your Custom Endpoints (for benchmarking)
# GPT Nano 4.1 Endpoint
CUSTOM_GPT_NANO_URL=https://your-gpt-nano-endpoint.com/v1/embeddings
CUSTOM_GPT_NANO_KEY=your-gpt-nano-api-key

# ADA-003 Small Text Embeddings Endpoint  
CUSTOM_ADA_003_URL=https://your-ada003-endpoint.com/v1/embeddings
CUSTOM_ADA_003_KEY=your-ada003-api-key

# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_API_VERSION=2024-02-01
AZURE_OPENAI_DEPLOYMENT_NAME=text-embedding-ada-002

# Optional: Public API Keys (for additional comparison)
# Only include if you want to test against public APIs
OPENAI_API_KEY=sk-your-openai-api-key
COHERE_API_KEY=your-cohere-api-key
VOYAGE_API_KEY=your-voyage-api-key

# Benchmark Configuration
BENCHMARK_ITERATIONS=10
BENCHMARK_TIMEOUT=30

# Note: The .env file is gitignored for security
# Only .env.example is committed to the repository