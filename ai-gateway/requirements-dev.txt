-r requirements.txt

# Testing and development
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
pytest-cov>=5.0.0

# Optional: model export (ONNX) & advanced tokenizer experiments
# These are NOT required for running the gateway, only for exporting models / research.
transformers>=4.45.0
optimum>=1.21.0
onnx>=1.16.0
